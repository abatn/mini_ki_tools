2) Die Provider-Liste ist statisch – Benutzer können keine API-Keys eingeben. Entwickle eine elegante Lösung: Jeder Provider in der Liste erhält ein eigenes Eingabefeld (type="password"), einen "Test"-Button zur Validierung und einen "Speichern"-Button. Die Keys werden verschlüsselt im Backend gespeichert (via POST /api/llm/keys) und bei erfolgreicher Validierung wird der Provider-Status auf "verfügbar" gesetzt. Bestätige mit "UI & PROVIDER FIXED". ** 
